# Railway PostgreSQL Configuration
# Pre lokálny vývoj: nastavte túto hodnotu v .env súbore
# Pre GitHub Actions: nastavte DATABASE_URL ako secret v GitHub repozitári
# Settings → Secrets and variables → Actions → New repository secret
DATABASE_URL=postgresql://user:password@hostname:port/database

# Local Configuration
NODE_ENV=development
PORT=3000

# Sync Configuration
SYNC_ENABLED=true
SYNC_INTERVAL=300000
AUTO_SYNC=true

