{
  "name": "pwms",
  "version": "1.0.0",
  "scripts": {
    "install": "chmod +x install_flutter.sh && bash install_flutter.sh",
    "build": "chmod +x build_vercel.sh && bash build_vercel.sh"
  }
}

