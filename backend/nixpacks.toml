[phases.setup]
# Node already includes npm; specifying npm-* can break on some nixpkgs pins.
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run migrate || true"]

[start]
cmd = "npm start"









