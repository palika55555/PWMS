# Copy to `.env` for local development (do not commit secrets).
# Railway sets DATABASE_URL automatically when you attach a Postgres plugin.
DATABASE_URL=postgresql://postgres:password@localhost:5432/pwms

# Optional (Railway sets this)
PORT=3000
NODE_ENV=production





