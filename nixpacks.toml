# Nixpacks konfigurácia pre Railway
# Tento súbor hovorí Railway, že toto je Node.js projekt, nie Flutter projekt

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "npm install"
]

[phases.build]
cmds = []

[start]
cmd = "node server.js"

[variables]
NODE_ENV = "production"

